{{#each blogs as |blog| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{blog.title}}</h2>
    <h3>created by: {{user.username}}</h3>
  </div>
  <div class="col-md-6">
    <h4>created on: {{format_date blog.createdAt}}</h4>
  </div>
</div>
{{/each}}
